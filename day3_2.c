#include<stdio.h>
int main()
{
    int l, b, area;
    printf("Enter the the values of length and bredth");
    scanf("%d %d",&l,&b);
    area=l*b;
    printf("Area of Rectangle=%d",area);
    return 0;
}